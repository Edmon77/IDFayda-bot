# ===========================================
# FAYDA BOT - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and fill in your values.
# For Railway: add these in Dashboard → Your Service → Variables
# ===========================================


# ---------- 1. TELEGRAM BOT (Required) ----------
# Get from: https://t.me/BotFather on Telegram
# Send /newbot, follow steps, then copy the token (looks like 123456789:ABCdefGHI...)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz


# ---------- 2. 2CAPTCHA API KEY (Required) ----------
# Get from: https://2captcha.com → Dashboard → API Key
# Used to solve reCAPTCHA for Fayda ID verification
CAPTCHA_KEY=your_32_character_2captcha_api_key


# ---------- 3. MONGODB URI (Required) ----------
# Option A - MongoDB Atlas (free tier):
#   1. Go to https://www.mongodb.com/cloud/atlas
#   2. Create account → Create Cluster (free M0)
#   3. Database Access → Add User (username + password)
#   4. Network Access → Add IP: 0.0.0.0/0 (allow from anywhere)
#   5. Clusters → Connect → Connect your application → Copy connection string
#   Replace <password> with your user password. Keep db name or use: fayda_bot
#
# Option B - Railway MongoDB:
#   Add MongoDB plugin in Railway → Variables tab will show MONGO_URL or MONGODB_URI
#   Use that value as-is (often: mongodb://mongo:27017)
#
# Format: must start with mongodb:// or mongodb+srv://
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/fayda_bot?retryWrites=true&w=majority


# ---------- 4. REDIS URL (Required) ----------
# Option A - Upstash (free tier, works great with Railway):
#   1. Go to https://upstash.com → Sign up
#   2. Create Database → Region: choose closest to your app
#   3. Copy "Redis URL" (starts with rediss:// or redis://)
#
# Option B - Railway Redis:
#   Add Redis plugin in Railway → Variables will show REDIS_URL
#   Copy that value
#
# Format: must start with redis:// or rediss:// (rediss = TLS)
REDIS_URL=rediss://default:YOUR_PASSWORD@us1-xxxxx.upstash.io:6379


# ---------- 5. SESSION SECRET (Required) ----------
# Any long random string. Used to sign sessions.
# Generate: run "openssl rand -hex 32" in terminal
# Or use: https://randomkeygen.com (CodeIgniter Encryption Keys)
SESSION_SECRET=a_long_random_string_at_least_32_characters


# ---------- 6. WEBHOOK DOMAIN (Required for deployment) ----------
# Your app's PUBLIC URL. Must be exact.
# Railway: Settings → Networking → your URL (e.g. https://idfayda-bot-production.up.railway.app)
# Include https://, NO trailing slash
WEBHOOK_DOMAIN=https://idfayda-bot-production.up.railway.app


# ---------- 7. SERVER (Optional - platforms often set these) ----------
# PORT: Railway/Render set automatically (e.g. 8080). Default 3000 for local.
PORT=3000
NODE_ENV=production


# ---------- 8. LOGGING (Optional) ----------
# LOG_LEVEL=info
