<!DOCTYPE html>
<html>
<head>
    <title>Super Admin Login — Fayda Bot</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, sans-serif; background: #0f0f12; color: #e4e4e7; min-height: 100vh; display: flex; justify-content: center; align-items: center; padding: 1rem; }
        .login-box { background: #18181c; border: 1px solid rgba(255,255,255,0.06); padding: 2rem; border-radius: 16px; width: 100%; max-width: 340px; }
        .login-box h2 { font-size: 1.25rem; margin-bottom: 0.5rem; }
        .login-box .sub { color: #a1a1aa; font-size: 0.875rem; margin-bottom: 1.5rem; }
        .error { color: #ef4444; font-size: 0.875rem; margin-bottom: 1rem; text-align: center; }
        input { width: 100%; padding: 0.75rem 1rem; margin-bottom: 1rem; background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; color: #e4e4e7; font-size: 1rem; }
        input:focus { outline: none; border-color: #6366f1; }
        button { width: 100%; padding: 0.75rem; background: #6366f1; color: white; border: none; border-radius: 8px; font-size: 1rem; font-weight: 500; cursor: pointer; }
        button:hover { background: #818cf8; }
    </style>
</head>
<body>
    <div class="login-box">
        <h2>Fayda Bot</h2>
        <p class="sub">Super admin dashboard</p>
        <% if (locals.error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        <form method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Log in</button>
        </form>
    </div>
</body>
</html>